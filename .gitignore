.cover/
coverage.out
*.out
findings.json
__pycache__/
.DS_Store
.agents/
.aios/
.claude/
.cline/
.cursor/
.goose/
.windsurf/

# Compiled binary from `go build -o aios ./cmd/aios`. Go binaries have no
# file extension, so no `*.ext` pattern can catch them — they must be
# ignored by path.
/aios
